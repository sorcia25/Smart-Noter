#!/usr/bin/env node
// TODO(phase-8.2): replace this stub with the real check that scans for hardcoded user-facing JSX strings.
// Phase 0 wired this hook before the implementation existed; this stub keeps the pre-commit pipeline green.
process.exit(0);
