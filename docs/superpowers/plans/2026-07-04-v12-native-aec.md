# v1.2 — AEC nativo de Windows Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cancelar el eco de las bocinas en grabaciones Mix delegando la cancelación al AEC nativo de Windows (modo `AudioCategory_Communications`), capturando el mic con WASAPI raw y fijando el endpoint de render del loopback como referencia del AEC.

**Architecture:** Un módulo nuevo `mic_comms.rs` abre el mic con `IAudioClient` en categoría Communications (crate `windows`), llama `IAcousticEchoCancellationControl::SetEchoCancellationRenderEndpoint`, y empuja f32 crudo al mismo `Sender<Vec<f32>>` que hoy usa cpal. El SO cancela el eco y compensa el clock drift internamente. `open()` elige la fuente del mic según `aec_enabled`; el mixer/writer/loopback no cambian. El `EchoCanceller` SpeexDSP queda dormido como fallback y se remueve post-smoke.

**Tech Stack:** Rust · crate `windows` 0.59 (`Win32_Media_Audio`) · WASAPI (`IAudioClient`/`IAudioCaptureClient`/`IAcousticEchoCancellationControl`) · crate `wasapi` 0.16 (resolución de endpoints) · React/TS (toggle).

---

## Convenciones (leer antes de empezar)

**Preámbulo de entorno — OBLIGATORIO en TODO comando `cargo`/`git`** (el pre-commit hook corre clippy/fmt sobre el workspace, que compila el crate de whisper y necesita libclang):

```bash
export PATH="$HOME/.cargo/bin:/c/Program Files (x86)/Microsoft Visual Studio/2022/BuildTools/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin:$PATH"
export LIBCLANG_PATH="C:/Program Files/LLVM/bin"
```

- Rama de trabajo: `feat/v12-native-aec` (ya creada, con el spec commiteado en `60cecc7`).
- El pre-commit hook (`lefthook`) corre `cargo clippy --workspace --all-targets -- -D warnings` + `cargo fmt --check` + `biome` en archivos staged. **Cero warnings permitidos.**
- Backend build/test acotado al crate: `cargo test -p smart-noter-audio`. Frontend: `pnpm test`.
- **`mic_comms.rs` es código COM de integración: NO es unit-testeable** (necesita endpoints de audio reales). Su validación es compilación + el smoke en hardware. Los tests unitarios cubren solo la lógica pura (selección de fuente, decisión de referencia).

## Estructura de archivos

| Archivo | Rol | Cambio |
|---|---|---|
| `src-tauri/crates/audio/src/capture/mic_comms.rs` | Captura del mic con AEC del SO (Communications) + helpers de decisión puros | **Crear** |
| `src-tauri/crates/audio/src/capture/mod.rs` | Declara submódulos de capture | Modificar (`mod mic_comms;`) |
| `src-tauri/crates/audio/src/capture/stream.rs` | Abre streams por modo | Modificar (`aec_enabled` en `open()`; `KeepAlive`/`WasapiStreamThread` a `pub(crate)`) |
| `src-tauri/crates/audio/src/capture/recorder.rs` | Orquesta el pipeline | Modificar (pasar `aec_enabled` a `open()`; quitar el bloque `mixer.enable_aec`) |
| `src-tauri/crates/audio/Cargo.toml` | Deps del crate | Modificar (agregar `windows`) |
| `src/features/pre-record/PreRecordPage.tsx` | Página de pre-grabación | Modificar (des-ocultar toggle) |
| `src/features/pre-record/PreRecordPage.test.tsx` | Tests de la página | Modificar (invertir el test del toggle) |
| `src-tauri/crates/core/src/models/settings.rs` | Settings persistidos | Modificar (doc-comment; default se voltea en Task 6) |
| `src-tauri/crates/audio/src/capture/mixer.rs` | Mixer | **Sin tocar** en dev; se limpia en Task 6 |
| `src-tauri/crates/audio/src/capture/echo_canceller.rs` | Cancelador SpeexDSP | **Se borra** en Task 6 (post-smoke) |

---

## Task 1: Helpers de decisión puros (TDD)

Crea `mic_comms.rs` con solo la lógica pura, testeable sin hardware: cuándo usar el mic comms, y cómo decidir la referencia del AEC (sentinel → auto).

**Files:**
- Create: `src-tauri/crates/audio/src/capture/mic_comms.rs`
- Modify: `src-tauri/crates/audio/src/capture/mod.rs`

- [ ] **Step 1: Declarar el módulo**

En `src-tauri/crates/audio/src/capture/mod.rs`, agrega junto a los otros `mod`:

```rust
pub(crate) mod mic_comms;
```

- [ ] **Step 2: Escribir el test que falla**

Crea `src-tauri/crates/audio/src/capture/mic_comms.rs` con:

```rust
//! Microphone capture through the Windows-native AEC (Communications signal
//! processing mode). Opens an `IAudioClient` in `AudioCategory_Communications`
//! and sets the loopback render endpoint as the echo-cancellation reference via
//! `IAcousticEchoCancellationControl`. The OS delivers an already-echo-cancelled
//! mic stream and compensates the loopback/mic clock drift internally.
//!
//! The captured f32 samples are pushed to the same `Sender<Vec<f32>>` the cpal
//! mic path uses, so the mixer/writer downstream are agnostic to the source.

use crate::capture::stream::DEFAULT_RENDER_LOOPBACK;
use crate::capture::session::CaptureMode;

/// Whether the microphone should be captured through the OS AEC (Communications
/// mode) instead of raw cpal. Only Mix mode has speaker echo to cancel.
pub(crate) fn use_comms_mic(mode: CaptureMode, aec_enabled: bool) -> bool {
    matches!(mode, CaptureMode::Mix) && aec_enabled
}

/// Whether the AEC reference should auto-follow the default render endpoint.
/// True for the "record whatever is playing" sentinel — we pass NULL to
/// `SetEchoCancellationRenderEndpoint` and let Windows track the default render
/// (so no manual re-set is needed when the output device changes). A pinned
/// loopback id resolves to that concrete endpoint instead.
pub(crate) fn aec_reference_is_auto(loopback_device_id: &str) -> bool {
    loopback_device_id == DEFAULT_RENDER_LOOPBACK
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn comms_mic_only_for_mix_with_aec() {
        assert!(use_comms_mic(CaptureMode::Mix, true));
        assert!(!use_comms_mic(CaptureMode::Mix, false));
        assert!(!use_comms_mic(CaptureMode::Mic, true));
        assert!(!use_comms_mic(CaptureMode::System, true));
    }

    #[test]
    fn sentinel_reference_is_auto_else_pinned() {
        assert!(aec_reference_is_auto(DEFAULT_RENDER_LOOPBACK));
        assert!(!aec_reference_is_auto("some-endpoint-id"));
    }
}
```

- [ ] **Step 3: Correr el test — debe fallar a compilar primero, luego pasar**

Run: `cargo test -p smart-noter-audio mic_comms`
Expected: PASS (2 tests). Si `CaptureMode` no deriva lo necesario o falta el `mod`, corrige hasta compilar.

- [ ] **Step 4: Commit**

```bash
git add src-tauri/crates/audio/src/capture/mic_comms.rs src-tauri/crates/audio/src/capture/mod.rs
git commit -m "feat(v12-aec): mic_comms module with pure source-selection helpers"
```

---

## Task 2: Captura WASAPI Communications + control de AEC (integración)

Agrega el dep `windows` e implementa `open_mic_comms`: resuelve el endpoint de captura, abre el `IAudioClient` en modo Communications, fija la referencia del AEC, y corre un hilo de captura que empuja f32 crudo al canal. **Código COM: validado por compilación + smoke, no por unit tests.**

**Files:**
- Modify: `src-tauri/crates/audio/Cargo.toml`
- Modify: `src-tauri/crates/audio/src/capture/stream.rs` (exponer `KeepAlive`/`WasapiStreamThread`)
- Modify: `src-tauri/crates/audio/src/capture/mic_comms.rs`

- [ ] **Step 1: Agregar el dep `windows`**

En `src-tauri/crates/audio/Cargo.toml`, bajo `[dependencies]`, agrega (después de `wasapi = "0.16"`):

```toml
windows = { version = "0.59", features = [
    "Win32_Media_Audio",
    "Win32_System_Com",
    "Win32_System_Threading",
    "Win32_Foundation",
] }
```

Nota: alinea la versión con la que `wasapi` 0.16 usa transitivamente si `cargo tree -p smart-noter-audio -i windows` muestra duplicados; un duplicado no rompe (no cruzamos punteros COM entre crates, solo Strings), pero es preferible una sola versión. Ajusta las features hasta que compile (windows-rs es quisquilloso con features).

- [ ] **Step 2: Exponer los keepalive de stream.rs para reuso**

En `src-tauri/crates/audio/src/capture/stream.rs`, cambia la visibilidad del trait `KeepAlive` y del struct `WasapiStreamThread` a `pub(crate)` para que `mic_comms` construya un `StreamHandle`:

- Línea ~72: `trait KeepAlive: Send {}` → `pub(crate) trait KeepAlive: Send {}`
- Línea ~86: `struct WasapiStreamThread {` → `pub(crate) struct WasapiStreamThread {`
- Sus campos `stop`/`handle`: agrégales `pub(crate)` (`pub(crate) stop: Arc<AtomicBool>,` y `pub(crate) handle: Option<std::thread::JoinHandle<()>>,`) para poder construirlo desde `mic_comms`.

Verifica que `StreamHandle` y sus campos ya sean `pub` (lo son) para poder construirlo desde `mic_comms`.

- [ ] **Step 3: Implementar `open_mic_comms` + el hilo de captura COM**

Agrega a `src-tauri/crates/audio/src/capture/mic_comms.rs` (arriba del bloque `#[cfg(test)]`). Este es el sketch fiel de la secuencia del sample oficial de Microsoft en windows-rs 0.59; **compila iterativamente** (nombres exactos de bindings/constantes se verifican con `cargo build`):

```rust
use crate::capture::stream::{KeepAlive, StreamHandle, WasapiStreamThread};
use crate::error::AudioError;
use crossbeam_channel::Sender;
use std::sync::atomic::{AtomicBool, AtomicU32, Ordering};
use std::sync::Arc;

use windows::core::{Interface, PCWSTR};
use windows::Win32::Foundation::{HANDLE, WAIT_OBJECT_0};
use windows::Win32::Media::Audio::{
    eCapture, eCommunications, AudioCategory_Communications, IAudioCaptureClient, IAudioClient,
    IAcousticEchoCancellationControl, IMMDevice, IMMDeviceEnumerator, MMDeviceEnumerator,
    AudioClientProperties, AUDCLNT_SHAREMODE_SHARED, AUDCLNT_STREAMFLAGS_EVENTCALLBACK, WAVEFORMATEX,
};
use windows::Win32::System::Com::{
    CoCreateInstance, CoInitializeEx, CLSCTX_ALL, COINIT_MULTITHREADED,
};
use windows::Win32::System::Threading::{CreateEventW, WaitForSingleObject, INFINITE};

/// Resolve the capture endpoint id string for the chosen mic (or the default
/// communications capture endpoint when `mic_device_id` is None). Reuses the
/// `wasapi` crate's friendly-name matching (mirrors `stream::resolve_render_device`
/// but for Direction::Capture) and returns the persistent endpoint id
/// (`IMMDevice::GetId`), which `IMMDeviceEnumerator::GetDevice` re-resolves.
fn resolve_capture_endpoint_id(mic_device_id: Option<&str>) -> Result<String, AudioError> {
    use crate::devices::{enumerate, AudioDeviceKind};
    use wasapi::{DeviceCollection, Direction};

    let dev = match mic_device_id {
        None => wasapi::get_default_device(&Direction::Capture).map_err(|e| match e {
            wasapi::WasapiError::Windows(inner) => AudioError::WasapiInit { hresult: inner.code().0 },
            other => AudioError::Other(format!("WASAPI get_default_device(capture): {other}")),
        })?,
        Some(our_id) => {
            let devices = enumerate()?;
            let target = devices
                .iter()
                .find(|d| d.id == our_id && d.kind == AudioDeviceKind::Input)
                .ok_or_else(|| AudioError::DeviceNotFound(our_id.to_string()))?;
            let coll = DeviceCollection::new(&Direction::Capture).map_err(|e| match e {
                wasapi::WasapiError::Windows(inner) => AudioError::WasapiInit { hresult: inner.code().0 },
                other => AudioError::Other(format!("WASAPI capture collection: {other}")),
            })?;
            let count = coll.get_nbr_devices().unwrap_or(0);
            let mut found = None;
            for i in 0..count {
                if let Ok(d) = coll.get_device_at_index(i) {
                    if d.get_friendlyname().ok().as_deref() == Some(target.name.as_str()) {
                        found = Some(d);
                        break;
                    }
                }
            }
            found.ok_or_else(|| AudioError::DeviceNotFound(our_id.to_string()))?
        }
    };
    dev.get_id().map_err(|e| AudioError::Other(format!("capture get_id: {e}")))
}

/// Open the microphone through the OS AEC (Communications mode).
///
/// `reference` is the loopback render endpoint id to use as the echo reference,
/// or None to let Windows auto-follow the default render (the sentinel case).
/// Returns a `StreamHandle` whose `sample_rate`/`channels` are the comms mix
/// format (the mixer resamples to 48k). f32 samples are pushed to `tx`.
pub(crate) fn open_mic_comms(
    mic_device_id: Option<&str>,
    reference: Option<String>,
    tx: Sender<Vec<f32>>,
    drops: Arc<AtomicU32>,
) -> Result<StreamHandle, AudioError> {
    let endpoint_id = resolve_capture_endpoint_id(mic_device_id)?;

    // Read the mix format up-front (activate + GetMixFormat, then release) so the
    // StreamHandle reports the real rate/channels; the capture thread re-activates
    // its own client (mirrors stream.rs loopback: read format up-front, thread
    // re-resolves). The comms mix format is stable for the endpoint.
    let (sample_rate, channels) = read_comms_mix_format(&endpoint_id)?;

    let stop = Arc::new(AtomicBool::new(false));
    let stop_thread = stop.clone();
    let drops_thread = drops.clone();
    let handle = std::thread::Builder::new()
        .name("wasapi-mic-aec".into())
        .spawn(move || {
            if let Err(e) = mic_comms_loop(&endpoint_id, reference, &tx, &drops_thread, &stop_thread) {
                tracing::error!("WASAPI comms-mic thread exited with error: {e}");
            }
        })
        .map_err(|e| AudioError::Other(format!("spawn comms-mic thread: {e}")))?;

    Ok(StreamHandle {
        sample_rate,
        channels,
        drops,
        mic_sample_rate: None,
        loop_sample_rate: None,
        loop_channels: None,
        mic_channels: None,
        _streams: vec![Box::new(WasapiStreamThread { stop, handle: Some(handle) }) as Box<dyn KeepAlive>],
    })
}

/// COM init for the current thread. S_OK: initialised. S_FALSE: already init in
/// the same mode (benign). RPC_E_CHANGED_MODE: the thread is already init in a
/// DIFFERENT apartment (e.g. the Tauri command thread is STA) — also fine, since
/// `IAudioClient` activation works in both STA and MTA. Any other failure is real.
/// This matters because `open_mic_comms` may run on a thread Tauri already
/// CoInitialize'd; only `mic_comms_loop`'s fresh thread is guaranteed clean MTA.
fn ensure_com() -> Result<(), AudioError> {
    const RPC_E_CHANGED_MODE: i32 = 0x8001_0106u32 as i32;
    // SAFETY: standard WASAPI init.
    let hr = unsafe { CoInitializeEx(None, COINIT_MULTITHREADED) };
    if hr.is_err() && hr.0 != RPC_E_CHANGED_MODE {
        return Err(AudioError::WasapiInit { hresult: hr.0 });
    }
    Ok(())
}

/// Get an IMMDevice for the endpoint id string via the enumerator.
unsafe fn device_for_id(endpoint_id: &str) -> Result<IMMDevice, AudioError> {
    let enumerator: IMMDeviceEnumerator =
        CoCreateInstance(&MMDeviceEnumerator, None, CLSCTX_ALL)
            .map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;
    let wide: Vec<u16> = endpoint_id.encode_utf16().chain(std::iter::once(0)).collect();
    enumerator
        .GetDevice(PCWSTR(wide.as_ptr()))
        .map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })
}

/// Activate a Communications-category client and read its mix format, then drop it.
fn read_comms_mix_format(endpoint_id: &str) -> Result<(u32, u16), AudioError> {
    ensure_com()?;
    unsafe {
        let device = device_for_id(endpoint_id)?;
        let client: IAudioClient = device
            .Activate(CLSCTX_ALL, None)
            .map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;
        set_comms_category(&client)?;
        let fmt = client.GetMixFormat().map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;
        let rate = (*fmt).nSamplesPerSec;
        let ch = (*fmt).nChannels;
        windows::Win32::System::Com::CoTaskMemFree(Some(fmt as *const _));
        Ok((rate, ch))
    }
}

/// Set `eCategory = AudioCategory_Communications` BEFORE Initialize (pulls the
/// AEC/Voice-Clarity APO into the capture chain). Must not use RAW.
unsafe fn set_comms_category(client: &IAudioClient) -> Result<(), AudioError> {
    let props = AudioClientProperties {
        cbSize: std::mem::size_of::<AudioClientProperties>() as u32,
        bIsOffload: false.into(),
        eCategory: AudioCategory_Communications,
        Options: Default::default(),
    };
    client
        .SetClientProperties(&props)
        .map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })
}

/// Capture loop: activate → comms category → initialize (event-driven) → set AEC
/// reference → start → drain buffers → push f32. Runs on its own thread with its
/// own COM apartment.
fn mic_comms_loop(
    endpoint_id: &str,
    reference: Option<String>,
    tx: &Sender<Vec<f32>>,
    drops: &Arc<AtomicU32>,
    stop: &Arc<AtomicBool>,
) -> Result<(), AudioError> {
    ensure_com()?;
    unsafe {
        let device = device_for_id(endpoint_id)?;
        let client: IAudioClient = device
            .Activate(CLSCTX_ALL, None)
            .map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;
        set_comms_category(&client)?;

        let fmt = client.GetMixFormat().map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;
        let channels = (*fmt).nChannels as usize;

        // 200 ms buffer (in 100-ns units), shared, event-driven.
        client
            .Initialize(AUDCLNT_SHAREMODE_SHARED, AUDCLNT_STREAMFLAGS_EVENTCALLBACK, 2_000_000, 0, fmt, None)
            .map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;
        windows::Win32::System::Com::CoTaskMemFree(Some(fmt as *const _));

        // Set the AEC reference (non-fatal: E_NOINTERFACE means AEC still runs,
        // OS auto-picks the reference).
        match client.GetService::<IAcousticEchoCancellationControl>() {
            Ok(aec) => {
                let hr = match &reference {
                    Some(id) => {
                        let wide: Vec<u16> = id.encode_utf16().chain(std::iter::once(0)).collect();
                        aec.SetEchoCancellationRenderEndpoint(PCWSTR(wide.as_ptr()))
                    }
                    None => aec.SetEchoCancellationRenderEndpoint(PCWSTR::null()),
                };
                if let Err(e) = hr {
                    tracing::warn!(hresult = e.code().0, "SetEchoCancellationRenderEndpoint failed; OS auto-reference");
                }
            }
            Err(e) => {
                tracing::info!(hresult = e.code().0, "no IAcousticEchoCancellationControl (E_NOINTERFACE); OS auto-reference");
            }
        }

        let event: HANDLE = CreateEventW(None, false, false, PCWSTR::null())
            .map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;
        client.SetEventHandle(event).map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;

        let capture: IAudioCaptureClient = client
            .GetService()
            .map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;

        client.Start().map_err(|e| AudioError::WasapiInit { hresult: e.code().0 })?;
        tracing::info!(channels, "WASAPI comms-mic (OS AEC) started");

        loop {
            if stop.load(Ordering::Relaxed) {
                let _ = client.Stop();
                return Ok(());
            }
            // Wait up to 100 ms so we can re-check the stop flag.
            if WaitForSingleObject(event, 100) != WAIT_OBJECT_0 {
                continue;
            }
            loop {
                if stop.load(Ordering::Relaxed) {
                    break;
                }
                let mut data: *mut u8 = std::ptr::null_mut();
                let mut frames: u32 = 0;
                let mut flags: u32 = 0;
                let hr = capture.GetBuffer(&mut data, &mut frames, &mut flags, None, None);
                if hr.is_err() || frames == 0 {
                    break;
                }
                // Comms mix format is IEEE float; decode interleaved f32.
                let n = frames as usize * channels;
                let samples: Vec<f32> = std::slice::from_raw_parts(data as *const f32, n).to_vec();
                let _ = capture.ReleaseBuffer(frames);
                if tx.try_send(samples).is_err() {
                    drops.fetch_add(1, Ordering::Relaxed);
                }
            }
        }
    }
}
```

Notas de compilación (verifica con `cargo build`):
- Los nombres exactos de `Activate`/`GetService`/`Initialize`/`GetBuffer` en windows-rs 0.59 pueden requerir argumentos `Option<*const>` o casts (`Interface::cast`). Ajusta hasta compilar; la SECUENCIA es la correcta.
- `IMMDevice::Activate` en windows-rs es genérico: `device.Activate::<IAudioClient>(CLSCTX_ALL, None)` o vía `&mut` out-param. Usa la forma que compile.
- Si falta alguna constante/feature, agrega la feature de `windows` correspondiente.

- [ ] **Step 4: Compilar (la validación de esta tarea)**

Run: `cargo build -p smart-noter-audio`
Expected: compila sin errores ni warnings (clippy limpio). Itera sobre nombres de bindings COM hasta que pase.

- [ ] **Step 5: Correr los tests existentes (no deben romperse)**

Run: `cargo test -p smart-noter-audio`
Expected: PASS (incluidos los 2 de Task 1). El código COM no tiene unit tests — se valida en el smoke.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/crates/audio/Cargo.toml src-tauri/crates/audio/src/capture/stream.rs src-tauri/crates/audio/src/capture/mic_comms.rs src-tauri/Cargo.lock
git commit -m "feat(v12-aec): WASAPI Communications-mode mic capture with OS AEC reference"
```

---

## Task 3: Cablear la selección de fuente en `open()` + quitar `mixer.enable_aec`

`open()` recibe `aec_enabled`; en Mix elige el mic comms (con fallback a cpal) y deriva la referencia del `device_id` del loopback. El recorder deja de pedir AEC al mixer.

**Files:**
- Modify: `src-tauri/crates/audio/src/capture/stream.rs`
- Modify: `src-tauri/crates/audio/src/capture/recorder.rs`

- [ ] **Step 1: Agregar `aec_enabled` a `open()` y ramificar el mic en Mix**

En `stream.rs`, cambia la firma de `open` (línea ~113) agregando `aec_enabled: bool` como último parámetro:

```rust
pub fn open(
    mode: CaptureMode,
    device_id: &str,
    mic_device_id: Option<&str>,
    tx_a: Sender<Vec<f32>>,
    tx_b: Option<Sender<Vec<f32>>>,
    on_device_change: Option<DeviceChangeCallback>,
    aec_enabled: bool,
) -> Result<StreamHandle, AudioError> {
```

Dentro del brazo `CaptureMode::Mix`, reemplaza el bloque que construye `mic_handle` (líneas ~135-141) por:

```rust
            let mic_handle = if crate::capture::mic_comms::use_comms_mic(CaptureMode::Mix, aec_enabled) {
                // OS AEC path: derive the echo reference from the loopback id
                // (sentinel → None so Windows auto-follows the default render).
                let reference = if crate::capture::mic_comms::aec_reference_is_auto(device_id) {
                    None
                } else {
                    // Pinned endpoint: resolve its persistent id as the reference.
                    resolve_render_device(device_id).ok().map(|(_, id)| id)
                };
                match crate::capture::mic_comms::open_mic_comms(
                    mic_device_id,
                    reference,
                    tx_b.clone(),
                    shared_drops.clone(),
                ) {
                    Ok(h) => h,
                    Err(e) => {
                        // Fallback: never record an un-cancelled mix silently —
                        // fall back to raw cpal and let the recorder's device-change
                        // channel surface it. Log loudly.
                        tracing::error!(?e, "OS AEC mic open failed; falling back to raw cpal mic");
                        match mic_device_id {
                            Some(mic_id) => {
                                let device = resolve_input_device(mic_id)?;
                                build_cpal_input_stream(device, tx_b, shared_drops.clone())?
                            }
                            None => open_mic_default_with_drops(tx_b, shared_drops.clone())?,
                        }
                    }
                }
            } else {
                match mic_device_id {
                    Some(mic_id) => {
                        let device = resolve_input_device(mic_id)?;
                        build_cpal_input_stream(device, tx_b, shared_drops.clone())?
                    }
                    None => open_mic_default_with_drops(tx_b, shared_drops.clone())?,
                }
            };
```

Nota: `tx_b.clone()` en la rama comms (y `tx_b` en la de fallback) — asegúrate de clonar `tx_b` donde se use dos veces para satisfacer el borrow checker; el `Sender` es `Clone`.

- [ ] **Step 2: Actualizar los call-sites de `open()`**

En `recorder.rs`, las DOS llamadas a `open(...)` (Mix ~línea 227 y else ~línea 393) reciben `aec_enabled` al final:

- Mix: `open(mode, &device_id, mic_device_id.as_deref(), a_tx, Some(b_tx), on_device_change, aec_enabled)?`
- else: `open(mode, &device_id, None, source_tx.clone(), None, on_device_change, false)?`  ← System/Mic nunca usan AEC.

En `stream.rs`, los tres tests que llaman `open(...)` (`open_mix_without_second_tx_returns_other` ~875, `open_system_with_unknown_device_returns_device_not_found` ~898, `default_render_sentinel_does_not_hit_device_not_found` ~934) reciben `false` como último argumento. Ejemplo:

```rust
let result = open(CaptureMode::Mix, "any-id", None, tx, None, None, false);
```

- [ ] **Step 3: Quitar el bloque `mixer.enable_aec` del recorder**

En `recorder.rs`, elimina por completo el bloque (líneas ~275-284):

```rust
                if aec_enabled {
                    if let Err(e) =
                        mixer.enable_aec(crate::capture::echo_canceller::EchoConfig::default())
                    {
                        tracing::error!(
                            ?e,
                            "AEC init failed; continuing without echo cancellation"
                        );
                    }
                }
```

(El OS ya canceló el eco en el carril del mic; el mixer no debe volver a cancelar. `aec_enabled` ahora viaja a `open()`.) El `mut mixer` sigue siendo `mut` porque `mixer.mix()` lo requiere.

- [ ] **Step 4: Compilar y correr tests**

Run: `cargo test -p smart-noter-audio`
Expected: PASS. Ajusta clones de `tx_b` si el borrow checker se queja. Clippy limpio.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/crates/audio/src/capture/stream.rs src-tauri/crates/audio/src/capture/recorder.rs
git commit -m "feat(v12-aec): route Mix mic through OS AEC when enabled; drop mixer SpeexDSP call"
```

---

## Task 4: Frontend — des-ocultar el toggle + invertir el test

**Files:**
- Modify: `src/features/pre-record/PreRecordPage.tsx`
- Modify: `src/features/pre-record/PreRecordPage.test.tsx`

- [ ] **Step 1: Invertir el test (que falle primero)**

En `PreRecordPage.test.tsx`, reemplaza el test de las líneas ~233-253 por:

```tsx
    // v1.2: native Windows AEC — the toggle is VISIBLE; enabling it records with
    // aecEnabled: true (routes the mic through the OS AEC).
    it('mix card shows the AEC toggle and records with aecEnabled true when enabled', async () => {
      setupWithSettings('system');
      const mixCard = await screen.findByRole('button', { name: /Sistema \+ Micrófono/ });
      await waitFor(() => expect(mixCard).toBeEnabled());
      await userEvent.click(mixCard);

      expect(await screen.findByLabelText('Micrófono de la mezcla')).toBeInTheDocument();
      const aecToggle = await screen.findByRole('checkbox', { name: 'Cancelar eco de bocinas' });
      expect(aecToggle).not.toBeChecked(); // default off during dev

      await userEvent.click(aecToggle); // enable AEC

      const startBtn = await screen.findByRole('button', { name: /Iniciar|Start/i });
      await userEvent.click(startBtn);
      expect(navigateSpy).toHaveBeenCalledOnce();
      const [, navOptions] = navigateSpy.mock.calls[0] as [
        string,
        { state: Record<string, unknown> },
      ];
      expect(navOptions.state.aecEnabled).toBe(true);
    });
```

- [ ] **Step 2: Correr el test — debe fallar**

Run: `pnpm test -- PreRecordPage`
Expected: FAIL — no existe el checkbox `Cancelar eco de bocinas` (aún oculto).

- [ ] **Step 3: Des-ocultar el toggle**

En `PreRecordPage.tsx`, reemplaza el comentario de la línea ~230:

```tsx
                {/* AEC toggle hidden — deferred to v1.2 (clock-drift limits, see EchoCanceller). */}
```

por el toggle restaurado (idéntico al de v1.1, que usa las keys i18n `aecToggleLabel`/`aecToggleHint` y la clase CSS `aecToggleRow` — verifica que sigan existiendo en `src/i18n/*` y en el CSS module; re-agrégalas si faltan):

```tsx
                <label className={styles.aecToggleRow}>
                  <input
                    type="checkbox"
                    checked={aecEnabled}
                    onChange={(e) => {
                      const v = e.target.checked;
                      setAecEnabled(v);
                      if (settings) void updateSettings({ ...settings, aecEnabled: v });
                    }}
                  />
                  <span>{t('aecToggleLabel')}</span>
                </label>
                <div className={styles.modeHint}>{t('aecToggleHint')}</div>
```

Y actualiza el comentario de las líneas ~63-64 (ya no está diferido):

```tsx
  // Native Windows AEC (v1.2): the OS cancels speaker echo in Mix mode when on.
  // Default off until the ship commit flips the persisted default (post-smoke).
  const [aecEnabled, setAecEnabled] = useState(false);
```

- [ ] **Step 4: Correr el test — debe pasar**

Run: `pnpm test -- PreRecordPage`
Expected: PASS. Si `aecToggleLabel`/`aecToggleHint`/`aecToggleRow` faltan, re-agrégalas (label ES = `Cancelar eco de bocinas`; hint ES ≈ `Con audífonos apágalo.`) y re-corre.

- [ ] **Step 5: Correr toda la suite de frontend + commit**

Run: `pnpm test`
Expected: PASS.

```bash
git add src/features/pre-record/PreRecordPage.tsx src/features/pre-record/PreRecordPage.test.tsx src/i18n
git commit -m "feat(v12-aec): unhide the speaker-echo-cancellation toggle (native AEC)"
```

---

## Task 5: Doc-comment de settings + verificación de bindings

**Files:**
- Modify: `src-tauri/crates/core/src/models/settings.rs`

- [ ] **Step 1: Actualizar el doc-comment de `aec_enabled`**

En `settings.rs` (líneas ~44-48), reemplaza el comentario que dice "DEFERRED to v1.2 and dormant" por:

```rust
    /// AEC (mix-mode speaker echo cancellation) via the Windows-native AEC
    /// (Communications signal-processing mode). When on, the mic is captured
    /// through the OS AEC in Mix mode. Default off until the v1.2 hardware smoke
    /// passes (the ship commit flips it on).
    #[serde(default)]
    pub aec_enabled: bool,
```

Deja el `Default` impl en `aec_enabled: false` por ahora (Task 6 lo voltea).

- [ ] **Step 2: Verificar que las bindings no cambian**

`aec_enabled` ya existe en `AppSettings` y en la firma de `start_recording`; no se agregaron comandos ni campos. Regenera y confirma que no hay diff:

Run: `cd src-tauri && cargo run --bin specta-export --features generate-bindings`
Expected: `src/ipc/bindings.ts` (gitignored) se regenera sin cambios funcionales. Vuelve a la raíz.

- [ ] **Step 3: Correr backend tests + commit**

Run: `cargo test -p smart-noter-core`
Expected: PASS (los tests de settings ya cubren el roundtrip; `aec_enabled` con `#[serde(default)]` no rompe blobs legacy).

```bash
git add src-tauri/crates/core/src/models/settings.rs
git commit -m "docs(v12-aec): settings aec_enabled now describes native OS AEC"
```

---

## Task 6: (POST-SMOKE) Remover el EchoCanceller SpeexDSP + activar por defecto + ship

**⚠️ NO EJECUTAR hasta que el smoke en hardware confirme que el AEC nativo funciona** (ver checklist abajo). Si el smoke falla, la Ruta B (EchoCanceller dormido) sigue disponible y esta tarea se pospone.

**Files:**
- Delete: `src-tauri/crates/audio/src/capture/echo_canceller.rs`
- Modify: `src-tauri/crates/audio/src/capture/mixer.rs`, `mod.rs`, `Cargo.toml`
- Modify: `src-tauri/crates/core/src/models/settings.rs`, `src/features/pre-record/PreRecordPage.tsx`

- [ ] **Step 1: Remover la integración AEC del mixer**

En `mixer.rs`: elimina el `use ...echo_canceller::{EchoCanceller, EchoConfig}` (línea ~81), el campo `echo: Option<EchoCanceller>` (~142), el campo `a_delayed: Vec<f32>` (~145) y su init (~192), el método `enable_aec` (~197), la rama AEC de `mix()` (~278-291, restaurando el mix directo sin `a_delayed`), y el test que llama `enable_aec` (~764). Ajusta `mix()` para mezclar `a_ready`/`b_ready` directamente como antes de la integración AEC.

- [ ] **Step 2: Borrar el módulo y el dep**

- Borra `src-tauri/crates/audio/src/capture/echo_canceller.rs`.
- En `capture/mod.rs`, quita `mod echo_canceller;`.
- En `Cargo.toml`, quita `aec-rs = "1.0.0"`.

- [ ] **Step 3: Voltear el default a on**

- En `settings.rs`: `Default` impl `aec_enabled: true`; y el `#[serde(default)]` → `#[serde(default = "default_true")]` (reusa el helper existente) para que blobs legacy también lo activen.
- En `PreRecordPage.tsx`: `useState(true)` y `settings.aecEnabled ?? true` (líneas ~65, ~67).
- En `PreRecordPage.test.tsx`: el test de Task 4 asume default off; actualízalo — el mock de settings ahora trae `aecEnabled: true`, o ajusta el aserto `not.toBeChecked()` → `toBeChecked()` y quita el click previo (ya viene on), verificando que igual navega con `aecEnabled: true`. Mantén un segundo caso apagándolo → `aecEnabled: false`.

- [ ] **Step 4: Full build + test + bump de versión**

Run: `cargo test --workspace` y `pnpm test`
Expected: PASS (verde). Sube la versión a `1.2.0` en `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml` (workspace `version`) y `package.json`, y actualiza el label de versión que muestra la UI (el mismo lugar que se tocó para 1.1.0).

- [ ] **Step 5: Regenerar baselines e2e si aplica + commit**

Run: `pnpm test:e2e:update` (solo si hubo shift visual; el toggle des-oculto puede mover el layout de la mix-card >2%). Revisa los PNG y commitea.

```bash
git add -A
git commit -m "feat(v12-aec): remove SpeexDSP EchoCanceller, enable native AEC by default, bump 1.2.0"
```

- [ ] **Step 6: Merge + tag + release** (vía `superpowers:finishing-a-development-branch`): merge `feat/v12-native-aec` → main (`--no-ff`, CI verde) → tag `v1.2.0` → `release.yml` → smoke de auto-update.

---

## Smoke en hardware (bloquea Task 6)

Correr desde el instalador buildeado (no dev-debug), en el equipo con bocinas:

1. **Mix + bocinas + AEC-on:** reproducir audio + hablar → grabación = voz + far-end, **sin doblado**.
2. ⭐ **Duración larga (5–10 min):** el eco NO reaparece con el tiempo (v1.1 falló a ~25 s por drift). El smoke DEBE correr largo.
3. **Audífonos + AEC-off:** voz cruda 48k completa.
4. **Cambio de salida a mitad** (follow-default): la referencia se auto-sigue, eco sigue cancelado, sin glitch.
5. **`E_NOINTERFACE` / mic raro:** fallback a cpal + no crashea.
6. **Transcripción:** el mic limpio no empeora Whisper.
7. **Playback:** la porción del mic procesada suena aceptable.

---

## Self-Review

**Cobertura del spec:**
- §3/§5 (mic comms + mismo canal + mixer agnóstico) → Tasks 1-3. ✓
- §4 (secuencia WASAPI/COM) → Task 2. ✓
- §6 (referencia NULL auto / id pinned) → Task 1 (`aec_reference_is_auto`) + Task 3 (derivación). ✓
- §7 (cadena completa, sin toggles por-efecto) → implícito (no se togglean efectos). ✓
- §8 (E_NOINTERFACE no-fatal; fallback con señal) → Task 2 (GetService) + Task 3 (fallback a cpal + log). ✓
- §9 (unit tests de lógica pura; smoke) → Task 1 (tests) + checklist de smoke. ✓
- §5 remoción diferida + default flip → Task 6 (post-smoke). ✓

**Placeholders:** el código COM de Task 2 es un sketch fiel marcado como compile-iterativo (inherente al COM raw sobre windows-rs); la secuencia es completa y correcta. Sin TODOs.

**Consistencia de tipos:** `open_mic_comms(mic_device_id: Option<&str>, reference: Option<String>, tx, drops) -> Result<StreamHandle>` usado igual en Task 2 (def) y Task 3 (call). `use_comms_mic`/`aec_reference_is_auto` def en Task 1, uso en Task 3. `open()` gana `aec_enabled: bool` (Task 3) y todos los call-sites (recorder ×2, tests ×3) se actualizan.

**Riesgo conocido:** los nombres exactos de bindings COM de windows-rs 0.59 (`Activate`/`GetService`/`GetBuffer`/`AudioClientProperties.Options`) se verifican al compilar en Task 2; la secuencia de llamadas está validada contra el sample oficial de Microsoft.
